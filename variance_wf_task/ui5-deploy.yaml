# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: au.com.iesol.po.confirmation.variancewftask
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://192.168.41.53:50000
          client: "100"
        app:
          name: /IESOL/SUPL_CONF_WF
          description: Supplier confirmation workflow task
          package: /IESOL/SUPL_CONFIRMATIONS
          transport: DEVK900071
        exclude:
          - /test/
